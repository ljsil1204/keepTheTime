<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/appointmentTabLayout"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginVertical="20dp"
            android:layout_marginHorizontal="15dp"
            android:background="@drawable/tab_bg"
            app:tabBackground="@drawable/tab_selected"
            app:tabIndicator="@null"
            app:tabSelectedTextColor="@color/basic_900"
            app:tabTextColor="@color/basic_400"
            app:tabUnboundedRipple="false"
            android:textSize="14sp"/>

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/appointmentViewPager2"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="90dp"/>

        <LinearLayout
            android:id="@+id/btnAppointmentAdd"
            android:layout_width="80dp"
            android:layout_height="82dp"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="15dp"
            android:layout_marginBottom="15dp"
            android:background="@drawable/shadow_add_btn"
            android:elevation="20dp"
            android:gravity="center">

            <ImageView
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/appointment_add_icon" />

        </LinearLayout>

    </FrameLayout>
</layout>